# Chapter 3 Nano Banana Pro QA Report

검수일: 2026-07-27
검수 방식: 각 최종 JPEG를 `view_image`로 원본 해상도에서 한 장씩 직접 확인
최종 판정: 12/12 PASS

## 공통 판정 기준

1. 과학적 화살표, 부호, 방향, 논리 관계가 명세와 일치한다.
2. 지정 한글·영문·숫자·기호만 보이며 오탈자와 명세 밖 텍스트가 없다.
3. 핵심 요소가 잘리거나 겹치지 않고, 그라데이션·그림자·광택·입체 색 면이 없다.
4. 명세의 핵심 패널과 비교 관계가 모두 존재한다.

모든 최종 산출물의 생성 모델은 `gemini-3-pro-image`이다. 재시도 횟수는 최초 생성 뒤 `--force`로 다시 생성한 횟수이며, fig01은 작업 시작 전에 존재하던 사용 로그 2건과 이번 작업의 강제 재생성 4회를 구분해 적었다.

## 도판별 결과

| Spec | 최종 파일 | 해상도 | 재시도 | 판정 및 PASS 근거 |
|---|---|---:|---:|---|
| `fig01.json` | `ch3_fig01_structure_layers.jpg` | 2752×1536 | 이번 작업 4회 (사용 로그 총 6회) | PASS — GPR, 화학량론 행렬 S, 구획, 운송, 경계, 바이오매스의 핵심 구조가 모두 있고 구조 정보가 조건이 명시된 GEM으로 이어진다. 행렬 셀에 임의 숫자·기호가 없고 전체가 흰 배경의 평면 선화다. |
| `fig02.json` | `ch3_fig02_model_scope_not_size.jpg` | 2752×1536 | 2회 | PASS — 축소 모델과 상세 모델의 범위·구획 해상도 차이만 보여 주며 우열·성능 점수·체크·X가 없다. 두 패널의 `릴리스`, `구획`, `경계 반응`, `집계 기준`은 장식 기호 없는 동일한 텍스트다. |
| `fig03.json` | `ch3_fig03_gpr_and_or.jpg` | 2752×1536 | 1회 | PASS — AND는 gA와 gB 필수 소단위가 완전한 복합체를 이뤄 반응 ON이 되고, OR는 두 독립 동위효소가 각각 반응 ON을 지지한다. 두 논리 관계가 혼동되지 않고 추가 라벨이 없다. |
| `fig04.json` | `ch3_fig04_single_gene_loss.jpg` | 2752×1536 | 3회 | PASS — gA 결손 시 AND는 gB가 남아도 반응 OFF, OR는 물리적으로 분리된 gB 단독 화살표로 반응 ON이다. 빨간 X는 각 패널의 gA에만 있고 효소 내부는 채우지 않았다. |
| `fig05.json` | `ch3_fig05_compartment_species.jpg` | 2752×1536 | 3회 | PASS — `pyr_e`, `pyr_c`, `pyr_m`이 세포외·세포질·미토콘드리아에 각각 분리되어 있고 세 종 사이에 자동 이동 화살표가 없다. 동일 피루브산 아이콘과 모든 구획은 색 면 없는 윤곽선이다. |
| `fig06.json` | `ch3_fig06_transport_column.jpg` | 2400×1792 | 3회 | PASS — `pyr_c → pyr_m` 운송 방향과 대응 포인터가 정확하며 위 행 `pyr_c −1 소비`, 아래 행 `pyr_m +1 생성`이 올바르다. 유니코드 마이너스가 보이고 두 행 표는 흰 내부의 평면 선화다. |
| `fig08.json` | `ch3_fig08_pts_group_translocation.jpg` | 2752×1536 | 4회 | PASS — 포도당은 세포외에서 세포질의 포도당 6-인산으로 변환되고, PEP는 피루브산으로 전환되며 PEP 선에서 운송선으로 향하는 위쪽 전달 화살표가 결합을 표시한다. `glc__D_e + pep_c → g6p_c + pyr_c`의 연속 밑줄 두 개와 철자가 정확하고 단독 `P`가 없다. |
| `fig09.json` | `ch3_fig09_exchange_sign.jpg` | 2752×1536 | 2회 | PASS — 저장 방향 `X_e → ∅`, 흡수 `v < 0`·`하한 < 0`, 분비 `v > 0`이 서로 일치한다. `(−10, 1000)`은 유니코드 마이너스를 사용하고 세 bounds 카드와 저장고에 추가 라벨·금지 기호가 없다. |
| `fig10.json` | `ch3_fig10_boundary_reactions.jpg` | 2752×1536 | 2회 | PASS — Exchange는 환경과 X_e의 흡수·분비, Demand는 X_c의 비가역 제거, Sink는 제거와 공급을 별도 단방향 화살표로 보여 준다. 빨간 X와 `근거 확인`은 Sink의 공급 방향에만 적용된다. |
| `fig11.json` | `ch3_fig11_biomass_three_layers.jpg` | 2752×1536 | 4회 | PASS — 바이오매스 반응, 선택적 의사대사물, 목적함수가 독립된 세 패널로 모두 존재한다. 목적함수는 세 빈 반응 카드 중 하나만 표적으로 선택되고 A와 C를 잇는 화살표가 없으며 모든 내부가 흰색이다. |
| `fig12.json` | `ch3_fig12_gam_ngam.jpg` | 2752×1536 | 3회 | PASS — GAM은 바이오매스 증가와 가수분해 아이콘 수가 비례하고, NGAM은 성장 정지선과 별개로 ATPM이 계속 작동한다. 숫자 `0`, `ATP hydrolysis`, 분자명 같은 추가 텍스트가 없고 두 단위가 정확하다. |
| `fig13.json` | `ch3_fig13_model_anatomy_audit.jpg` | 2752×1536 | 4회 | PASS — GPR→구획→운송→경계→바이오매스→조건이 명시된 모델의 점선 순서가 완전하다. 아이콘에 추가 논리·장식 기호가 없고 모델 릴리스·배지·목적함수·허용오차가 모두 있으며 카드와 태그는 색 면 없는 선화다. |

## 게시 승인

모든 도판은 위 네 가지 공통 기준을 통과했다. 불합격 중간 산출물은 게시 대상에서 제외하며, 위 표의 최종 파일만 Chapter 3 자산으로 게시한다.
