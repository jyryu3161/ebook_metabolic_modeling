# Chapter 10 신규 그림 배치안

이 문서는 Chapter 10 번호 절 `01.md`–`15.md`의 기존 시각화와 Nano Banana Pro용 신규 spec을 합친 계획 수, 삽입 위치, 대체 텍스트와 자립 캡션 초안을 기록합니다. 현재 API key를 사용하지 않으므로 이미지를 생성하지 않았고, 과학·한글·상세도 검수를 통과하기 전에는 본문 이미지 링크를 추가하지 않습니다. 신규 spec은 모두 `final_prompt_info.md`의 공통 STYLE을 전치해 생성하는 교육용 독립 모식도이며 외부 그림을 복제하거나 재구성하지 않습니다.

## 시각화 수 집계

기존 수는 `scripts/validate_figure_coverage.py`와 같은 기준으로 Markdown 이미지와 Mermaid fence만 셉니다. 표와 실행할 때만 나타나는 Plotly·ipywidgets 출력은 기존 정적 시각화 수에 포함하지 않습니다.

| 절 | 기존 Markdown 이미지 | 기존 Mermaid | 기존 합계 | 신규 spec | 계획 합계 |
|:---:|---:|---:|---:|---:|---:|
| 01 | 0 | 0 | 0 | 2 | 2 |
| 02 | 0 | 0 | 0 | 2 | 2 |
| 03 | 0 | 0 | 0 | 2 | 2 |
| 04 | 0 | 0 | 0 | 2 | 2 |
| 05 | 0 | 0 | 0 | 2 | 2 |
| 06 | 0 | 0 | 0 | 2 | 2 |
| 07 | 0 | 0 | 0 | 2 | 2 |
| 08 | 0 | 0 | 0 | 2 | 2 |
| 09 | 3 | 0 | 3 | 0 | 3 |
| 10 | 0 | 0 | 0 | 2 | 2 |
| 11 | 1 | 0 | 1 | 1 | 2 |
| 12 | 1 | 0 | 1 | 1 | 2 |
| 13 | 0 | 0 | 0 | 2 | 2 |
| 14 | 0 | 0 | 0 | 2 | 2 |
| 15 | 0 | 0 | 0 | 2 | 2 |
| **합계** | **5** | **0** | **5** | **26** | **31** |

## 번호 운용

장 도입의 기존 Mermaid는 그림 10.1로 유지합니다. 신규 그림을 본문 앞에서부터 삽입할 때 `01.md`–`08.md`는 그림 10.2–10.17을 사용하고, `09.md`의 기존 3열 정적 비교는 그림 10.18로 바꿉니다. `10.md` 신규 그림은 10.19–10.20, `11.md` 기존 production envelope와 신규 그림은 10.21–10.22, `12.md` 기존 robustness 곡선과 신규 그림은 10.23–10.24, `13.md`–`15.md` 신규 그림은 10.25–10.30으로 이어집니다. 기존 캡션 번호 변경, 본문 링크 삽입과 `FIGURE_SOURCES.md` 갱신은 생성 결과가 검수를 통과한 뒤 한 번에 수행합니다.

## `chapter-10/01.md`

### `fig02.json` — 계획 번호 그림 10.2

- **삽입 위치:** 0.0절의 실행 배지 표 다음.
- **목적:** 네 종류의 코드 블록이 요구하는 입력과 실제 실행 여부를 구분합니다.
- **대체 텍스트 초안:** 독립 실행, 선행 셀 필요, 외부 자산 필요, 의사코드의 네 카드와 독립 실행에서 선행 셀 필요로 이어지는 핵심 실행 경로.
- **자립 캡션 초안:** _그림 10.2:_ Chapter 10의 코드 블록을 독립 실행, 선행 셀 필요, 외부 자산 필요와 의사코드로 구분한 실행 계약입니다. 핵심 순차 실습은 필요한 입력을 자체 준비하는 셀에서 시작해 앞 셀의 `model`·`results`를 받는 셀로 이어지며, 외부 SBML과 의사코드는 선택 경로입니다. 실제 노트북 상태나 계산값을 표시하지 않은 교육용 모식도입니다. 저자 작성; 구현 근거: [Jupyter 공식 문서](https://docs.jupyter.org/en/latest/), [COBRApy 공식 문서](https://opencobra.github.io/cobrapy/).

### `fig03.json` — 계획 번호 그림 10.3

- **삽입 위치:** 0.1절 첫 Python 확인 셀 다음.
- **목적:** 예상과 다른 출력이 나왔을 때 초보자가 확인할 순서를 고정합니다.
- **대체 텍스트 초안:** 출력이나 assert 이상에서 커널 재시작, 위에서부터 재실행, COBRApy 버전과 가상환경 커널 확인으로 이어지는 진단 흐름.
- **자립 캡션 초안:** _그림 10.3:_ 출력이나 `assert`가 예상과 다를 때 커널을 재시작하고 위에서부터 다시 실행한 뒤 COBRApy 0.30.0과 `.venv`의 Python 경로를 확인하는 순서입니다. 이 절차는 남은 메모리 상태와 환경 불일치를 먼저 분리하지만, 모델 입력 자체의 오류까지 자동으로 해결한다는 뜻은 아닙니다. 실제 오류 로그를 복제하지 않은 교육용 진단 모식도입니다. 저자 작성; 구현 근거: [Jupyter 공식 문서](https://docs.jupyter.org/en/latest/), [Python `venv` 문서](https://docs.python.org/3/library/venv.html).

## `chapter-10/02.md`

### `fig04.json` — 계획 번호 그림 10.4

- **삽입 위치:** 1.1절 시작 셀의 기대 출력 다음.
- **목적:** 버전·solver·모델 식별 정보가 계산 전에 묶여야 함을 보여 줍니다.
- **대체 텍스트 초안:** Python, COBRApy 0.30.0, GLPK, textbook 모델과 단일 process 입력이 assert 점검을 거쳐 버전, solver 인터페이스와 모델 개수 출력으로 이어지는 구조.
- **자립 캡션 초안:** _그림 10.4:_ Python·COBRApy·GLPK·`textbook` 모델과 process 수를 입력으로 고정하고, `assert`로 solver 인터페이스·모델 ID·반응 95개·대사물 72개·유전자 137개를 확인하는 preflight 흐름입니다. 세 개수는 COBRApy 0.30.0이 적재한 `e_coli_core` 릴리스의 식별값이지 대장균 종 전체의 고정 특성이 아닙니다. 실제 실행 화면이 아닌 교육용 절차 모식도입니다. 저자 작성; 구현 근거: [COBRApy configuration 문서](https://cobrapy.readthedocs.io/en/latest/configuration.html), [BiGG `e_coli_core`](http://bigg.ucsd.edu/models/e_coli_core).

### `fig05.json` — 계획 번호 그림 10.5

- **삽입 위치:** 1.2절 SBML 코드 다음.
- **목적:** 예제 모델과 사용자 모델의 ID·기준값을 섞지 않게 합니다.
- **대체 텍스트 초안:** `load_model('textbook')` 경로와 사용자 SBML 경로가 각자 ID·구획·GPR을 점검한 뒤 서로 다른 기준값으로 이어지는 비교.
- **자립 캡션 초안:** _그림 10.5:_ 내장 `textbook` 모델은 BiGG ID와 이 장의 반응 ID를 확인한 뒤 기준값을 재현하지만, 사용자 SBML은 파일 존재·exchange·biomass·구획·GPR을 새로 확인한 뒤 자체 기준값을 만들어야 합니다. 두 경로 사이에서 반응 ID와 수치 기준을 그대로 복사하면 안 됨을 나타낸 교육용 전환 모식도이며 실제 사용자 모델을 표시하지 않습니다. 저자 작성; 구현 근거: [COBRApy 입출력 문서](https://cobrapy.readthedocs.io/en/latest/io.html), [SBML 공식 사이트](https://sbml.org/).

## `chapter-10/03.md`

### `fig06.json` — 계획 번호 그림 10.6

- **삽입 위치:** 2.1절 첫 문단 다음.
- **목적:** `Model`·`Reaction`·`Metabolite`·`Gene`의 탐색 방향을 한눈에 보여 줍니다.
- **대체 텍스트 초안:** 중앙 Model과 Reaction, Metabolite, Gene 카드가 양방향으로 연결되고 Reaction 카드에 반응식·bounds·GPR이 표시된 객체 관계도.
- **자립 캡션 초안:** _그림 10.6:_ COBRApy `Model`이 `Reaction`, `Metabolite`, `Gene` 목록을 보유하고, 반응에서 대사물·유전자로 또는 유전자에서 관련 반응으로 이동할 수 있는 객체 연결입니다. `DictList.get_by_id()`는 대소문자를 구분하므로 `pgi`와 `PGI`는 같은 조회가 아닙니다. 특정 경로의 생물학적 연결이나 실제 객체 수를 표시하지 않은 교육용 객체 모식도입니다. 저자 작성; 구현 근거: [COBRApy core objects 문서](https://cobrapy.readthedocs.io/en/latest/getting_started.html).

### `fig07.json` — 계획 번호 그림 10.7

- **삽입 위치:** 2.2절 `model.medium` 기대 출력 다음.
- **목적:** exchange flux 부호와 `medium`의 양수 섭취 용량 표기를 구분합니다.
- **대체 텍스트 초안:** 세포로 들어가는 섭취 flux 음수, 밖으로 나가는 분비 flux 양수와 `bounds`의 -10이 `model.medium`의 섭취 용량 10에 대응하는 도식.
- **자립 캡션 초안:** _그림 10.7:_ 이 절의 BiGG형 exchange 정의에서는 음의 flux가 섭취, 양의 flux가 분비를 나타내지만, `model.medium`은 섭취 용량의 양수 크기를 표시합니다. 따라서 glucose lower bound `-10`과 medium 값 `10`은 부호만 다른 같은 용량 정보이며 실제 최적 flux를 뜻하지 않습니다. COBRApy `textbook`의 부호 규약을 설명하는 교육용 모식도입니다. 저자 작성; 구현 근거: [COBRApy media 문서](https://cobrapy.readthedocs.io/en/latest/media.html).

## `chapter-10/04.md`

### `fig08.json` — 계획 번호 그림 10.8

- **삽입 위치:** 3.1절 기대 출력 다음.
- **목적:** FBA를 선형대수 유도 대신 입력·코드·출력·해석 순서로 읽게 합니다.
- **대체 텍스트 초안:** 화학량론 S, 반응 bounds, biomass 목적 입력이 `model.optimize()`를 거쳐 status, objective value, fluxes로 나뉘고 상태 점검 뒤 해석으로 이어지는 흐름.
- **자립 캡션 초안:** _그림 10.8:_ FBA는 화학량론, 반응 경계와 목적함수를 입력으로 받아 `model.optimize()`로 풀고 `status`, 목적값과 반응별 flux를 반환합니다. `optimal` 상태와 유한한 수치를 먼저 확인한 뒤 생물학적으로 해석하며, 최적 상태만으로 실험적 사실이 확정되지는 않습니다. 실제 GEM 결과값이나 solver 내부 알고리듬을 표시하지 않은 교육용 실행 모식도입니다. 저자 작성; 개념 근거: [Orth et al. (2010)](https://doi.org/10.1038/nbt.1614), 구현 근거: [COBRApy 공식 문서](https://opencobra.github.io/cobrapy/).

### `fig09.json` — 계획 번호 그림 10.9

- **삽입 위치:** 3.2절 질량보존 코드 다음.
- **목적:** 행렬 곱을 배우지 않은 독자도 잔차의 입력과 의미를 따라가게 합니다.
- **대체 텍스트 초안:** 대사물 X의 생성률과 소비률 차이, 화학량론 행렬 S와 flux 벡터의 곱, 최대 절댓값 잔차의 허용오차 판정.
- **자립 캡션 초안:** _그림 10.9:_ 대사물 하나에서는 생성 반응 flux와 소비 반응 flux의 차이가 잔차이며, 전체 모델에서는 화학량론 표 `S`와 반응별 flux를 곱해 모든 대사물의 생성률−소비률을 동시에 계산합니다. 최대 절댓값이 `1e-9`보다 작다는 판정은 이 실행의 수치 허용오차 안에서 질량보존을 만족한다는 뜻이며 열역학적 평형을 뜻하지 않습니다. 실제 계산값을 표시하지 않은 교육용 검산 모식도입니다. 저자 작성; 개념 근거: [Orth et al. (2010)](https://doi.org/10.1038/nbt.1614).

## `chapter-10/05.md`

### `fig10.json` — 계획 번호 그림 10.10

- **삽입 위치:** 4.1절 잘못된 들여쓰기 예제 다음.
- **목적:** `with model:` 안과 밖의 모델 상태를 구분합니다.
- **대체 텍스트 초안:** 같은 Model 객체가 블록 전 기본 배지, 블록 안 임시 배지·목적, 블록 후 복원된 배지·목적으로 바뀌는 시간 흐름.
- **자립 캡션 초안:** _그림 10.10:_ `with model:`은 새 모델을 만드는 대신 같은 객체의 배지·경계·목적 변경을 블록 안에서만 유지하고 블록을 벗어날 때 복원합니다. 임시 조건의 결과는 블록 안에서 계산해야 하며, 밖에서 계산하면 복원된 기준 조건을 다시 풀게 됩니다. 실제 메모리 구조나 계산값을 표시하지 않은 교육용 상태 모식도입니다. 저자 작성; 구현 근거: [COBRApy model context 문서](https://cobrapy.readthedocs.io/en/latest/autoapi/cobra/core/model/index.html).

### `fig11.json` — 계획 번호 그림 10.11

- **삽입 위치:** 4.2절 기대 출력 다음.
- **목적:** 같은 모델에서도 목적함수에 따라 출력 질문이 달라짐을 보여 줍니다.
- **대체 텍스트 초안:** 같은 화학량론·bounds·배지에서 biomass 최대화와 ATPM 최대화가 각각 성장률과 유지 ATP 능력 출력으로 갈라지는 구조.
- **자립 캡션 초안:** _그림 10.11:_ 같은 `S`·반응 경계·배지에서 biomass를 최대화하면 성장 능력을, `ATPM`을 최대화하면 유지 ATP 반응의 최대 허용 flux를 묻습니다. 두 출력은 목적함수가 다른 조건부 예측이므로 같은 생물학적 양으로 비교하지 않으며, `with model:` 뒤에는 biomass 목적이 복원됩니다. 실제 모델 수치를 표시하지 않은 교육용 목적 전환 모식도입니다. 저자 작성; 개념 근거: [Orth et al. (2010)](https://doi.org/10.1038/nbt.1614).

## `chapter-10/06.md`

### `fig12.json` — 계획 번호 그림 10.12

- **삽입 위치:** 5.1절 기대 출력 다음.
- **목적:** pFBA의 두 단계와 `objective_value`·biomass flux를 분리합니다.
- **대체 텍스트 초안:** biomass 최대화로 최대 성장률을 고정한 뒤 총 절대 flux를 최소화하고, 성장률과 마지막 단계 목적값을 서로 다른 출력 열에서 읽는 흐름.
- **자립 캡션 초안:** _그림 10.12:_ pFBA는 먼저 최대 biomass flux를 구하고, 그 값을 유지한 채 총 절대 flux가 작은 대표 해를 선택합니다. COBRApy의 반환값에서 성장률은 biomass 반응 flux로 읽고 마지막 단계 목적값은 총 flux 기준으로 읽어야 하며, 총 flux를 실제 효소 질량으로 해석하지 않습니다. 실제 GEM 계산값을 표시하지 않은 교육용 2단계 모식도입니다. 저자 작성; 개념 근거: [Lewis et al. (2010)](https://doi.org/10.1038/msb.2010.47).

### `fig13.json` — 계획 번호 그림 10.13

- **삽입 위치:** 5.2절 기대 출력 다음.
- **목적:** FVA의 각 최소·최대가 별도 계산이라는 점을 시각화합니다.
- **대체 텍스트 초안:** 공통 모델·90퍼센트 성장 조건에서 PGI, PFK, TPI를 각각 최소화·최대화해 세 구간을 만들고 모든 minimum을 하나의 벡터로 묶는 해석을 차단한 도식.
- **자립 캡션 초안:** _그림 10.13:_ FVA는 공통 모델·배지와 목적 수준 아래에서 관심 반응을 하나씩 별도로 최소화·최대화하여 반응별 허용 구간을 만듭니다. 각 구간 끝점은 서로 다른 최적화의 결과이므로 모든 반응의 minimum을 모은 값이 하나의 공동 flux 상태라는 보장은 없고, 통계적 신뢰구간도 아닙니다. 실제 수치 구간을 표시하지 않은 교육용 계산 구조입니다. 저자 작성; 개념 근거: [Mahadevan & Schilling (2003)](https://doi.org/10.1016/j.ymben.2003.09.002).

## `chapter-10/07.md`

### `fig14.json` — 계획 번호 그림 10.14

- **삽입 위치:** 6.0절 의사코드 다음.
- **목적:** 유전자 ID에서 조건부 성장 분류까지의 자료 흐름을 보여 줍니다.
- **대체 텍스트 초안:** 유전자 ID, GPR 평가, 반응 bounds 변경, FBA 재계산, 성장 비율 분류로 이어지고 마지막에 세 분류로 갈라지는 파이프라인.
- **자립 캡션 초안:** _그림 10.14:_ 단일 유전자 결손은 유전자 ID에서 GPR을 평가해 관련 반응 경계를 바꾸고, 같은 배지·목적함수에서 FBA를 다시 풀어 wild-type 대비 성장 비율을 분류합니다. `or`는 한 유전자가 남으면 반응이 유지될 수 있고 `and`는 필요한 소단위 하나가 없으면 반응이 차단될 수 있음을 뜻합니다. 실제 필수 유전자나 실험 생존을 표시하지 않은 교육용 파이프라인입니다. 저자 작성; 개념 근거: [Orth et al. (2010)](https://doi.org/10.1038/nbt.1614), 구현 근거: [COBRApy deletion 문서](https://cobrapy.readthedocs.io/en/latest/deletions.html).

### `fig15.json` — 계획 번호 그림 10.15

- **삽입 위치:** 6.1절 기대 출력 다음.
- **목적:** `NaN`을 status 확인 없이 0으로 바꾸는 오류를 막습니다.
- **대체 텍스트 초안:** 결손 출력의 유한성 확인, 비유한 값의 infeasible 상태 확인, 명시적 0 치환 정책과 세 분류로 이어지는 결정 흐름.
- **자립 캡션 초안:** _그림 10.15:_ 결손 성장값이 유한하면 성장 비율을 계산하고, 비유한 값이면 먼저 solver 상태가 `infeasible`인지 확인한 뒤에만 이 실습의 분류 정책에 따라 0으로 바꿉니다. `optimal`인데 값이 비유한 경우에는 생물학적 필수성으로 분류하지 않고 데이터·계산 오류를 조사합니다. 실제 결손 수나 유전자 ID를 표시하지 않은 교육용 안전 처리 모식도입니다. 저자 작성; 구현 근거: [COBRApy deletion 문서](https://cobrapy.readthedocs.io/en/latest/deletions.html).

## `chapter-10/08.md`

### `fig16.json` — 계획 번호 그림 10.16

- **삽입 위치:** 7.0절 세 방법 설명 다음.
- **목적:** 결손 전 WT 기준 flux의 계산 순서와 참조 범위를 분리하면서, 동일 mutant에 대한 세 선택 기준을 비교합니다.
- **대체 텍스트 초안:** 같은 textbook 모델과 기본 배지에서 결손 전 WT 기준 flux를 먼저 계산해 선형 MOMA와 선형 ROOM에만 참조로 넣고, 별도의 tpiA 결손 mutant 가지를 FBA·선형 MOMA·선형 ROOM 모두에 적용하는 비교.
- **자립 캡션 초안:** _그림 10.16:_ 같은 `textbook` 모델·기본 배지에서 결손 전 wild-type flux를 먼저 계산하고, 이 기준은 선형 MOMA와 선형 ROOM에만 참조 입력으로 사용합니다. 별도로 만든 `tpiA` 결손 mutant는 FBA·선형 MOMA·선형 ROOM에 공통 적용되며, FBA는 biomass 최대화, MOMA는 WT와의 flux 차이 합 최소화, ROOM은 큰 변화 최소화를 선택 기준으로 사용합니다. 세 방법의 성장률은 biomass flux에서 비교하고 방법 고유 목적값은 직접 비교하지 않습니다. 실제 실행 수치를 표시하지 않은 교육용 방법 비교 모식도입니다. 저자 작성; 개념 근거: [Segrè et al. (2002)](https://doi.org/10.1073/pnas.232349399), [Shlomi et al. (2005)](https://doi.org/10.1073/pnas.0406346102).

### `fig17.json` — 계획 번호 그림 10.17

- **삽입 위치:** 7.1절 결과 표 다음.
- **목적:** 공통 비교 열과 방법별 목적값 열을 분리해 읽게 합니다.
- **대체 텍스트 초안:** FBA, 선형 MOMA, 선형 ROOM 세 행에서 biomass flux가 공통 비교 열에 정렬되고 biomass, flux 차이 합, 허용 범위 이탈 목적값은 별도 열에 놓인 표형 도식.
- **자립 캡션 초안:** _그림 10.17:_ FBA·선형 MOMA·선형 ROOM 결과에서 생물학적 관심 출력인 `Biomass_Ecoli_core` flux는 공통 열에서 비교하고, biomass 목적값·flux 차이 합·허용 범위 이탈은 방법별 정의와 단위를 붙여 별도로 기록합니다. MOMA와 ROOM 목적값의 숫자 크기는 방법의 우열을 뜻하지 않습니다. 실제 수치를 포함하지 않은 교육용 결과 읽기 모식도입니다. 저자 작성; 개념 근거: [Segrè et al. (2002)](https://doi.org/10.1073/pnas.232349399), [Shlomi et al. (2005)](https://doi.org/10.1073/pnas.0406346102).

## `chapter-10/09.md`

신규 spec은 없습니다. 현재 3열 정적 비교에 Markdown 이미지 3개가 있어 최소 2개를 충족합니다. 생성·삽입 단계에서는 현행 그림 10.2 캡션을 그림 10.18로 바꾸되, 세 PNG가 한 비교 그림을 구성한다는 캡션 구조는 유지합니다.

## `chapter-10/10.md`

### `fig19.json` — 계획 번호 그림 10.19

- **삽입 위치:** 9.1절 기대 출력 다음.
- **목적:** gap-filling 전·후의 입력과 선택 결과를 장난감 네트워크에서 직접 연결합니다.
- **대체 텍스트 초안:** SOURCE_A에서 A까지 연결되지만 A와 B 사이가 끊긴 추가 전 네트워크, 후보 R_AB 선택, R_AB 추가 뒤 BIOMASS까지 연결된 네트워크.
- **자립 캡션 초안:** _그림 10.19:_ 장난감 모델은 `SOURCE_A`와 `BIOMASS` 사이의 `A → B` 반응이 빠져 목적 flux가 0이고, 후보 풀의 `R_AB`를 선택해 추가하면 공급 상한 안에서 목적 flux가 회복됩니다. 이는 본문 코드의 입력·선택·출력 관계를 보여 주는 교육용 네트워크이며 실제 생물의 성장이나 반응 증거를 나타내지 않습니다. 저자 작성; 구현 근거: [COBRApy gap-filling 문서](https://cobrapy.readthedocs.io/en/latest/gapfilling.html).

### `fig20.json` — 계획 번호 그림 10.20

- **삽입 위치:** 9.2절 첫 문단 다음.
- **목적:** gap-filling 선택을 생물학적 증거로 오독하지 않게 합니다.
- **대체 텍스트 초안:** gap-filling 선택 반응이 유전체 주석, 문헌, 열역학 방향, 세포 구획의 네 검토 관문을 거쳐 채택 또는 보류·거절로 갈라지는 흐름.
- **자립 캡션 초안:** _그림 10.20:_ gap-filling이 선택한 반응은 목표 flux를 회복하는 계산 후보이며, 유전체 주석·문헌 근거·열역학적 방향·세포 구획을 검토한 뒤 근거와 confidence를 기록해 채택하거나 후보 풀과 penalty를 바꾸어 다시 계산합니다. 네 검토 항목을 통과했다는 사실만으로 생물학적 진실이 증명되는 것은 아닙니다. 실제 큐레이션 결과를 표시하지 않은 교육용 근거 관문입니다. 저자 작성; 개념 근거: [Thiele & Palsson (2010)](https://doi.org/10.1038/nprot.2009.203).

## `chapter-10/11.md`

기존 production envelope PNG는 생성 후 계획 번호 그림 10.21로 바꿉니다.

### `fig22.json` — 계획 번호 그림 10.22

- **삽입 위치:** 10.1절 coupling 계산 해석 문단 다음, `해석상의 주의` 상자 앞.
- **목적:** 고정하는 반응과 다시 최적화하는 목적함수를 바꾸면 두 포락선의 세로축 의미가 달라짐을 보여 줍니다.
- **대체 텍스트 초안:** acetate를 고정하고 성장률 최소·최대를 구하는 패널과 biomass를 고정하고 acetate 최소·최대를 구하는 패널, 양의 성장에서 최소 acetate가 0인 지점.
- **자립 캡션 초안:** _그림 10.22:_ 왼쪽은 acetate flux를 고정하고 성장률의 최소·최대를 계산해 생산–성장 trade-off를 읽고, 오른쪽은 biomass flux를 고정하고 acetate의 최소·최대를 계산해 growth coupling을 점검합니다. 오른쪽의 양의 성장 지점에서 최소 acetate가 0이면 주어진 모델·배지·grid에서는 강제 생산 결합을 입증하지 못합니다. 수치 눈금과 실제 계산값을 사용하지 않은 교육용 축 해석 모식도입니다. 저자 작성; 구현 근거: [COBRApy production envelope 문서](https://cobrapy.readthedocs.io/en/latest/phenotype_phase_plane.html).

## `chapter-10/12.md`

기존 glucose robustness PNG는 생성 후 계획 번호 그림 10.23으로 바꿉니다.

### `fig24.json` — 계획 번호 그림 10.24

- **삽입 위치:** 11.2절 첫 문단 다음.
- **목적:** 위젯 탐색값과 재현 가능한 고정 조건 기록을 구분합니다.
- **대체 텍스트 초안:** glucose·oxygen 슬라이더, `with model` 안 임시 성장 계산, 선택한 두 값의 고정, 배지·목적함수·solver·결과 JSON 기록으로 이어지는 흐름.
- **자립 캡션 초안:** _그림 10.24:_ glucose와 oxygen 슬라이더는 `with model:` 안에서 임시 조건을 탐색하고 함수가 끝나면 모델을 복원합니다. 최종 분석에 채택한 조건은 숫자로 다시 고정하고 배지·목적함수·solver·결과와 함께 JSON에 저장해야 하며, 위젯의 화면 상태만으로는 재현 기록이 되지 않습니다. 실제 slider 값이나 성장률을 표시하지 않은 교육용 탐색–기록 모식도입니다. 저자 작성; 구현 근거: [ipywidgets 공식 문서](https://ipywidgets.readthedocs.io/).

## `chapter-10/13.md`

### `fig25.json` — 계획 번호 그림 10.25

- **삽입 위치:** 12.1절 기대 출력 다음.
- **목적:** SBML 저장과 왕복 검증의 다섯 확인 항목을 묶습니다.
- **대체 텍스트 초안:** 메모리 모델을 SBML로 저장하고 SHA-256을 만든 뒤 다시 읽어 status, 반응 수, 대사물 수, 목적함수, 최적값을 비교하는 순환.
- **자립 캡션 초안:** _그림 10.25:_ 메모리의 COBRApy 모델을 SBML로 저장하고 파일 SHA-256을 기록한 뒤 다시 읽어 solver 상태·반응 수·대사물 수·목적함수·최적값을 비교하는 왕복 검증입니다. 저장 함수가 성공했다는 사실만으로 모델 의미가 보존되었다고 보지 않으며, 실제 파일 해시나 계산값을 표시하지 않습니다. 저자 작성; 형식 근거: [SBML 공식 사이트](https://sbml.org/), 구현 근거: [COBRApy 입출력 문서](https://cobrapy.readthedocs.io/en/latest/io.html).

### `fig26.json` — 계획 번호 그림 10.26

- **삽입 위치:** 12.2절 첫 문단 다음.
- **목적:** 바이트 동일성과 모델 의미 비교를 분리합니다.
- **대체 텍스트 초안:** SHA-256으로 XML 바이트 동일성을 확인하는 패널과 반응·대사물·bounds·목적함수·최적값을 비교하는 모델 의미 패널.
- **자립 캡션 초안:** _그림 10.26:_ SHA-256은 두 파일의 바이트 배열이 같은지 확인하고, 모델 의미 비교는 반응·대사물·경계·목적함수·최적값을 직접 대조합니다. 같은 해시는 같은 파일을 가리키지만, serializer 순서나 메타데이터 때문에 해시가 달라도 모델 계산 내용이 같을 수 있으므로 두 검사를 서로 대체하지 않습니다. 실제 해시 문자열을 사용하지 않은 교육용 비교 모식도입니다. 저자 작성; 형식 근거: [NIST Secure Hash Standard](https://csrc.nist.gov/pubs/fips/180-4/upd1/final), [SBML 공식 사이트](https://sbml.org/).

## `chapter-10/14.md`

### `fig27.json` — 계획 번호 그림 10.27

- **삽입 위치:** 13.0절 JSON 코드 다음.
- **목적:** provenance JSON에 환경·모델·조건·결과·해석 정책을 함께 넣게 합니다.
- **대체 텍스트 초안:** 환경, 모델, 조건, 결과, 해석 정책의 다섯 층과 생성 시점 태그가 있는 JSON 문서.
- **자립 캡션 초안:** _그림 10.27:_ 재현 기록 JSON은 Python·패키지·solver 환경, 모델 출처와 식별값, 배지·경계·목적함수 조건, 상태와 관심 결과, 임계값·비유한 값 처리·허용오차 정책을 분리해 저장합니다. 생성 시각은 실행 기록의 시점이지 모델 release 시점이 아니며, 실제 실행 JSON이나 수치를 복제하지 않은 교육용 기록 구조입니다. 저자 작성; 재현성 근거: [Sandve et al. (2013)](https://doi.org/10.1371/journal.pcbi.1003285).

### `fig28.json` — 계획 번호 그림 10.28

- **삽입 위치:** 13.1절 기준값 표 다음.
- **목적:** 재실행에 필요한 입력·실행 환경·결과 기록의 묶음을 보여 줍니다.
- **대체 텍스트 초안:** SBML·checksum·배지 입력, 코드·패키지·solver·seed 실행 정보, JSON·표·그림·정책 기록이 독립 재실행과 허용오차 비교로 모이는 구조.
- **자립 캡션 초안:** _그림 10.28:_ 독립 재실행에는 SBML과 checksum, 배지·경계 입력, 코드·패키지 버전·solver·seed, 결과 JSON·표·그림과 해석 정책이 함께 필요합니다. 재실행 결과는 solver 상태를 확인하고 명시한 허용오차 안에서 비교하며, 노트북의 셀 출력만 보존한 것은 충분한 전달 묶음이 아닙니다. 실제 프로젝트 파일을 표시하지 않은 교육용 인계 모식도입니다. 저자 작성; 재현성 근거: [Sandve et al. (2013)](https://doi.org/10.1371/journal.pcbi.1003285), [Wilkinson et al. (2016)](https://doi.org/10.1038/sdata.2016.18).

## `chapter-10/15.md`

### `fig29.json` — 계획 번호 그림 10.29

- **삽입 위치:** 14.0절 번호 목록 다음.
- **목적:** 탄소원 또는 유전자 하나를 선택해 완결된 분석으로 연결하는 흐름을 요약합니다.
- **대체 텍스트 초안:** 탄소원과 유전자 중 한 조건을 선택한 뒤 환경 고정, 임시 적용, 상태·질량보존, pFBA·FVA, 그림·해석, SBML·JSON 저장으로 이어지는 워크플로.
- **자립 캡션 초안:** _그림 10.29:_ 탄소원 또는 유전자 조건 하나를 선택하고 모델·환경을 고정한 뒤 `with model:`로 조건을 적용하여 solver 상태와 질량보존을 검산하고, pFBA·FVA·그림 해석을 거쳐 SBML과 JSON을 저장하는 미니 프로젝트 흐름입니다. 상태 검사가 실패하면 결과를 해석하지 않고 조건과 입력 점검으로 돌아갑니다. 실제 연구 결과를 표시하지 않은 교육용 분석 절차입니다. 저자 작성; 개념 근거: [Orth et al. (2010)](https://doi.org/10.1038/nbt.1614), 재현성 근거: [Sandve et al. (2013)](https://doi.org/10.1371/journal.pcbi.1003285).

### `fig30.json` — 계획 번호 그림 10.30

- **삽입 위치:** “이 장, 그리고 이 책을 마치며” 제목 앞.
- **목적:** 계산 출력에서 조건부 주장과 실험 비교로 가는 근거 사슬을 마무리합니다.
- **대체 텍스트 초안:** 모델·배지 입력, 목적함수·solver 계산, status·질량보존 검산, FVA·방법 가정 불확실성, 조건부 예측 보고와 별도 실험 관찰 비교의 연결.
- **자립 캡션 초안:** _그림 10.30:_ 모델·버전과 배지·경계를 입력하고 목적함수와 solver로 계산한 뒤 상태·질량보존·유한성을 검산하고, FVA 범위와 방법 가정을 붙여 조건부 예측으로 보고하는 근거 사슬입니다. 실험 관찰은 같은 endpoint와 조건을 맞추어 별도로 비교하며 계산 결과가 관찰을 자동으로 확정하지 않습니다. 실제 GEM 수치나 실험 자료를 사용하지 않은 교육용 종합 모식도입니다. 저자 작성; 개념 근거: [Orth et al. (2010)](https://doi.org/10.1038/nbt.1614), 재현성 근거: [Sandve et al. (2013)](https://doi.org/10.1371/journal.pcbi.1003285).

## 생성 후 필수 검수

1. 각 JSON에 `final_prompt_info.md`의 공통 STYLE을 전치해 2K 결과를 생성합니다.
2. 과학 검수에서 exchange 부호, GPR `and`/`or`, pFBA의 두 출력, FVA의 반응별 독립 구간, 결손 status 분기, MOMA·ROOM 목적값 분리, gap-filling 전후 연결, production envelope의 고정 축과 SBML 해시 의미를 확인합니다.
3. 한글을 음절 단위로 판독하고 오타·깨짐·환각 텍스트·잘림·잘못 연결된 화살표를 검사합니다.
4. 입력–코드–출력–생물학적 해석의 네 요소가 spec에서 빠지지 않았는지, 실제 GEM 계산이나 실험 관찰로 오인할 가짜 수치·화면이 없는지 확인합니다.
5. 검수를 통과한 뒤에만 본문 링크·대체 텍스트·최종 캡션·장 전역 번호와 `FIGURE_SOURCES.md`를 함께 갱신합니다.
